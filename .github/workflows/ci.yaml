name: github.ci
on:
  push:
    branches: [ main ]

jobs:
  hello:
    runs-on: go-os
    steps:
      - uses: actions/checkout@v2

      - name: Run a multi-line script
        run: |
          date
          uname -a